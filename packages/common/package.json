{
  "name": "@ecom/common",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.json && tsc-alias -p tsconfig.json --resolve-full-paths",
    "start": "bun dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5",
    "bun-types": "latest"
  },
  "dependencies": {
    "@grpc/proto-loader": "^0.8.0",
    "@types/jsonwebtoken": "^9.0.10",
    "ioredis": "^5.8.2",
    "jsonwebtoken": "^9.0.3",
    "winston": "^3.19.0",
    "zod": "^4.2.0"
  }
}
